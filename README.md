# Obstacle-avoiding-robot---Arduino-UNO

This is the code for a obstacle avoiding robot. 
This robot can navigate around the environment using a ultrasonic sound sensor
It can be manually controlled using bluetooth device commands (android app)
It can be manually controlled by voice commands using bluetooth

